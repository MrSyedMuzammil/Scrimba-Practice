function addressMaker(address) {
  const newAddress = {
    city: address.city,
    state: address.state,
    country: "Pakistan",
  };
}

// addressMaker({ city: "Peshawar", state: "KPK" });

// Refactor Code to use Object Destructuring and
